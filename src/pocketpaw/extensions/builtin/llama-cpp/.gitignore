# Python sandbox
env/

# Downloaded models
models/*.gguf
models/*.bin

# Node modules (UI source + node-llama-cpp engine)
ui/node_modules/
node_modules/

# Vite build output (regenerated by `cd ui && npx vite build`)
assets/
index.html

# Old vanilla files (replaced by Vite build)
app.js
styles.css
